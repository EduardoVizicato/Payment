import { PaymentDetailRequest } from './payment-detail-request.model';

describe('PaymentDetailRequest', () => {
  it('should create an instance', () => {
    expect(new PaymentDetailRequest()).toBeTruthy();
  });
});
